# coursework-RISC-V-kernel
Implementation of a simple OS, built for the RISC-V architecture. The set of supported concepts include memory allocation, threads, semaphores, synchronous context switching and asynchronous switching via timer and keyboard interrupts. The RISC-V runtime environment for the OS is the QEMU emulator. Spec: Operating Systems 1 2024. project spec  
